# XH-Axon 环境变量配置示例

# ============================================
# 开发环境配置 (.env)
# ============================================

# API 配置
VITE_API_BASE_URL=http://localhost:3100
VITE_API_TIMEOUT=10000

# 应用配置
VITE_APP_NAME=XH-Axon
VITE_APP_VERSION=0.2.1

# 环境
VITE_NODE_ENV=development

# ============================================
# 生产环境配置 (.env.production)
# ============================================

# API 配置 - 生产环境使用 /api 路径，通过 nginx 代理
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT=10000

# 应用配置
VITE_APP_NAME=XH-Axon
VITE_APP_VERSION=0.2.1

# 环境
VITE_NODE_ENV=production

# ============================================
# 使用说明
# ============================================

# 1. 复制此文件为 .env（开发环境）
#    cp .env.example .env

# 2. 复制此文件为 .env.production（生产环境）
#    cp .env.example .env.production

# 3. 根据实际环境修改对应的变量值

# 4. 在代码中使用：
#    const apiUrl = import.meta.env.VITE_API_BASE_URL

# 注意：
# - 所有环境变量必须以 VITE_ 开头才能在前端代码中使用
# - 修改环境变量后需要重启开发服务器
# - 生产环境构建时会读取 .env.production 文件
